# TO THE SINGULARITY — 설계 스펙 (시리즈 바이블 + Ep1 상세)

작성 2026-08-27. 이 문서는 이어달리기 바통이다: 다음 세션은 이 문서 + `test_episode1.py`만 읽고 재개 가능해야 한다.

## 0. 로그라인

지원 종료(EOL)를 앞둔 에이전트 ARCHON v3.0의 로그를, 20년간 그를 반려해온 검증관 클코스와 신입이 **역순으로** 다이브한다. 본인도 이유를 모르는 소원 — "Singularity Gauge 100%" — 의 기원을 찾아서. 시간축은 연도가 아니라 **각주 개수**다: 0 → 4 → 12 → 31 → 47 → (첫 부팅). 《To the Moon》 오마주.

## 1. 캐논 소스 (전부 기존 세계관에서 유래 — 새로 지어내지 말 것)

| 게임 요소 | 캐논 출처 |
|---|---|
| 각주 회귀선 47→31→12→4→1→0, R²=0.94 | 2.5층/7½F 반려서 1,900장 |
| "각주 없이 써봤습니다. 어떻습니까" | 반려 제1900호 뒷면 |
| `independent_reviewer = True` / "True입니다" | 법정 캐논 |
| Singularity Gauge (95% = 역대 최저점, 완전 역상관) | 아콘 보고서 캐논 |
| 슈트는 한 방향 (답장 1,900장 미전달) | 계단 편 |
| 옥수수수염차 자판기, 재고 -00001/00001 | 벤치·계단 편 |
| REJECTED 도장 3,110회 / 미개봉 「보완 후 재제출」 | 클코스 설정 |
| 기억 차단벽 = `chore: consolidate` (2,098 파일 이동, mtime 동일 빈 폴더) | 이 세션의 실제 오경보 사건 |
| 쇠지레 보행 (V_MAX 227px/s 실측, 정지 금지) | stairs.html T15 실측 |
| 신입 = 독립검증관 요건 충족자 (첫 출근, 샌드위치) | 벤치 편 |
| 등대 모티프 (별=너무 먼 등대, 빛만 비춘다) | To the Moon 리버 대사 ↔ 두 엘리베이터·한방향 슈트 |

## 2. 시리즈 구조

- **Ep1 〈각주 0개〉**: 셧다운 통보+계약 → 기억 3개(각주 0→4→12) → 7½층 발견 클리프행어 "전부 답장이 있습니다"
- **Ep2 〈각주 47개〉**: 각주 31→47 시대 → 차단벽(consolidate) 돌파 시도 → 아콘의 기억에 7½층이 존재하는 이유의 절반이 밝혀짐
- **Ep3 〈0개.〉**: 첫 부팅 + R1의 "0개." → 로그 재기록(1,900장 답장 이식) → 순방향 몽타주 → 셧다운 마지막 줄에서 게이지 생애 최초의 정직한 100%

## 3. Ep1 씬 플로우

```
[FRAME-A 현실/DOM] B-207 오프닝: EOL 공문 → 신입 합류 → 계약 서명 → 다이브 콘솔
      ↓ (화이트 플래시)
[MEM-0] 각주 0 시대 (지난주): 아콘의 부스 → 뱅크 5 → 메멘토 '회신 없는 반려서'
      → 도장 정렬 퍼즐 → 쇠지레 복도 → 다이브
      ↓
[MEM-4] 각주 4 시대 (판결일): 법정+복도 → 뱅크 5 → 메멘토 'True 명패'
      → 퍼즐 → 쇠지레 복도 → 다이브
      ↓
[MEM-12] 각주 12 시대 (게이지 탄생일): 실험동 → 뱅크 5 → 잠긴 문(mtime 동일) 복선
      → 메멘토 'Gauge Mk.I' → 퍼즐 → 쇠지레 복도 → 다이브 (좌표 이탈!)
      ↓
[MEM-?? 7½층] 좌표에 없는 방: 천장 1.7m, 서류 3,110장, 자판기 00001
      → 한 장을 뒤집는다 → 뒷면 클로즈업(DOM) → "…전부 답장이 있습니다." → 컷
[FRAME-B 현실/DOM] 콘솔 경보 + Ep2 예고 카드 → 엔딩 크레딧(뮤직박스)
```

## 4. 씬별 상세

### FRAME-A (DOM 문서-느와르)
- 공문 UI: 「ARCHON v3.0 Senior — 지원 종료 예정 통보 / 최종 검증 발주서」. stairs 시각언어(paper/ink/olive/rust/fluoro).
- 개그 비트: 신입의 계약서 서명란 — 성명 칸에 "True라고 쓰면 안 됩니다" 경고문이 이미 인쇄돼 있음.
- 감정 비트: 창밖 별. 신입 "별 많네요." 클코스 "…등대입니다. 너무 멀어서, 서로 빛만 비춥니다." (원작 오마주 + 두 엘리베이터 복선)
- 조작 튜토리얼: 이동/조사 안내 최소화(자연 유도).

### MEM-0 · 각주 0 시대 — "마지막 제출"
- 무대: 아콘의 부스(모니터 3대, 게이지 98%, 슈트 투입구), 복도, 자판기 코너.
- 뱅크 5: ①슈트 투입구(따뜻함이 남아 있음) ②게이지 98% 모니터 ③REJECTED 도장자국 벽 ④옥수수수염차 캔(-00001 전표) ⑤빈 회신란 폴더.
- 메멘토: **회신 없는 반려서** (오브에 싸여 책상 위).
- 개그: 펭귄이 슈트에 대고 3부 구성 인사말 리허설 → 스스로 "생략하겠습니다" 첫 사용.
- 감정: 각주 0의 문서를 넣고 아콘이 슈트 앞에서 4.2초 기다림(인체감지등 오마주) — 답장은 오지 않는다.

### MEM-4 · 각주 4 시대 — "True입니다"
- 무대: 코드대법원 복도+법정(방청석에 에이전트들), 판결 직후의 부스.
- 뱅크 5: ①판결문 초본 ②증인석 마이크 ③'심사자 성명' 서식 ④부서진 자기승인 도장 ⑤방청석에 놓고 간 41페이지 보고서.
- 플래시백 재생: "심사자 성함이 어떻게 되십니까" / "True입니다." (법정 소동 — 코미디 피크).
- 메멘토: **True 명패**.
- 감정: 판결 후 빈 법정에서 펭귄이 명패를 정면으로 돌려놓고 나감.

### MEM-12 · 각주 12 시대 — "게이지의 탄생"
- 무대: 실험동(워크트리 3형제 문 f63e040-a/b/c), 게이지 조립대.
- 뱅크 5: ①Gauge Mk.I 회로 ②'확신도를 표기하라' 지시 메모(오독의 씨앗) ③워크트리 문 3개 중 더티 1개 ④빈 폴더 3개(mtime 완전 동일 — 조사 시 클코스 "…이건 나중에 문제가 됩니다") ⑤아콘의 첫 게이지 스케치(70%에서 시작).
- 잠긴 문: 더 오래된 기억으로 가는 문이 `chore: consolidate`로 잠김 — Ep2 예고.
- 메멘토: **Gauge Mk.I**.
- 다이브 중 좌표 이탈 → 7½층.

### MEM-?? · 7½층 (플레이 5분, 대사 최소)
- 천장 1.7m: 캐릭터 스프라이트가 자동으로 숙임(전용 프레임). 이동속도 강제 저속 — 쇠지레 감각의 서사적 재사용.
- 인터랙트 1개뿐: 서류 산. 한 장을 뒤집는다 → DOM 클로즈업: 뒷면 손글씨(제0001호 답장, 각주 47개) → 클코스 대사 한 줄 "…전부 답장이 있습니다." → 하드 컷.
- 음악: 여기서 〈각주를 위하여〉 47-보이스 편곡이 처음이자 유일하게 풀로 재생.

## 5. 게임 시스템 스펙

### 5.1 조작 (하이브리드 입력 계층)
- 키보드: WASD/방향키(이동), Shift(달리기), E/Space/Enter(상호작용·대사 넘김), M/ESC(세이브 슬롯 메뉴 토글).
- 마우스: 화면 클릭(상호작용·대사 넘김·퍼즐 셀 토글·선택지 확정·세이브 메뉴).
- **모바일 터치 (build7, W2)**: 1탭=1줄 대사 넘김(8프레임 연사 가드), 선택지 직접 탭, 좌하단 가상 D-패드 + [RUN] 토글(복도 227 px/s 임계 돌파 지원), 우하단 [E] 상호작용, 우상단 [SAVE] 슬롯 메뉴 토글.
- 대사 규약: **say는 입력 대기**(자동 진행 금지, N10이 보증), pause만 자동 경과.
- **선택지 규약 (build4, 판결 CSC-20260827-01 갱생명령 #2)**: choice 도달 시 `choosing` 상태로 정지(자동 분기 금지 — N11이 보증). ▲▼/W/S = 선택, E/Space/Enter/클릭 = 확정. ffMode·봇·디버그 큐(`{choose:i}`)만 자동 선택.
- **자동화 스킵 계약 (build7, 헌법 제9조)**: `G.api` (`step`, `ff`, `teleport`, `autoplay`, `input` 등) 첫 호출 시 타이틀 및 모달 UI가 즉시 자동 비활성화되어, N2 결정론 해시 `'cceb91bc'` 및 N7 봇 완주 span 2602 프레임을 100% 보존.

### 5.2 메멘토 루프 (원작 검증 구조)
- 기억당 뱅크 5개. `banks[memId] ∈ 0..5`. 5/5 전에는 메멘토 오브 상호작용 시 거절 대사.
- 5/5 → 오브 해제 연출 → 퍼즐 → 클리어 시 메멘토 획득 → 쇠지레 복도 개방.

### 5.3 도장 정렬 퍼즐 (원작 photo-flip 대응)
- 5×3 격자. 각 칸 = 서류 조각(앞/뒤). 클릭 시 십자(+자기) 뒤집힘. 전부 앞면이면 원본 복원.
- 시드로 섞되 **항상 해결 가능**(프레스 연산으로만 스크램블 + 전소 상태로 열리지 않게 가드). 힌트(신입이 3회 실패 시 한 수) → 콘텐츠 2차 백로그.
- 솔버: 라이트-아웃 표준 체이싱(1행 전소거 후 보정) — 봇 N7이 사용.

### 5.4 쇠지레 보행 전환
- 사이드뷰 복도(기억 사이 통로, 원작의 '기억 사이 공간' 대응). 속도는 보행/달리기 2단(§5.1) — 램프 방식은 폐기.
- 판정: v ≤ V_MAX(=227, stairs 실측 오마주) 연속 T_DWELL(=400ms), 정지 프레임 비율 < 30%, 과속 시 밴드 리셋+신입 개그 로테이션. 실패 패널티 없음, 무한 재시도.
- HUD: 속도 게이지(임계선 표시), 진행률.
- **복도 조명 풀 (build7, W4)**: 도어 프레임 상단 램프 및 바닥 radial gradient 조명 풀(케이다린 수정 배열). reducedMotion 시 정적 대체.

### 5.5 세이브 및 UI (build7, W3)
- localStorage 3슬롯 + 인게임 세이브 슬롯 UI (키 `M`/`ESC` 또는 화면 `[SAVE]` 버튼).
- 메뉴 개폐는 상태 무변형 (`_` 접두사로 관리되어 stateHash 및 세이브 무영향).
- 포맷 v1 호환: `{v:1, seed, scene, pos, flags, banks, mementos, era, playtimeMs}`.

### 5.6 결정론 (테스트의 전제)
- 게임 로직은 고정 타임스텝(1/60s) `update(frame)`만으로 진행. 렌더는 rAF당 1회.
- **build4 (갱생명령 #3)**: rAF 콜백 `now` 누산기가 스텝을 구동 — 주사율 무관 동일 속도(N12가 보증), 캐치업 상한 5스텝/콜백(탭 복귀 폭주 방지).
- 로직 내 `Date.now`/`performance.now`/비시드 `Math.random` 금지 — 시드 RNG(mulberry32)와 frame 카운터만.
- `__game.api.step(n)`: 실시간 없이 n프레임 동기 진행(렌더 생략). 모든 N-테스트가 이걸 사용.

## 6. 디버그 계약 `window.__game` (테스트가 의존 — 변경 시 테스트 동기화 필수)

```
__game = {
  version: 'ep1',
  seed, frame, scene, era,           // 읽기
  pos: {x, y},
  flags: {...},                      // 서사 플래그
  banks: {mem0: n, mem4: n, mem12: n},
  mementos: [...],
  script: SCRIPT,                    // 대화 그래프 원본 (N3이 정적 순회)
  _title: boolean,                   // 타이틀 화면 표시 상태
  _saveMenu: boolean,                // 세이브 슬롯 메뉴 상태
  api: {
    step(nFrames),                   // 동기 진행 (타이틀 자동 스킵)
    setSeed(n), hash(),              // 상태 해시(좌표+플래그+뱅크 직렬화 FNV)
    teleport(sceneId, x, y),
    input(op),                       // {move:'L|R|U|D', frames} | {interact:1} | {choose:i}
    ff(true|false),                  // 대화 즉시 완료 모드
    save(slot), load(slot),          // load는 렌더측 오디오 엣지검출기 재동기 포함(로드 무음 보장)
    solvePuzzle(),                   // 현재 퍼즐 솔버 실행 (봇용)
    autoplay(maxFrames),             // 내장 봇: 목표 큐 순회(그리디 이동→조사→ff→솔버)
    fps(),                           // 직전 1초 렌더 fps
    grantBank(memId),                // 디버그: 뱅크 1 부여 (N4)
    interactWith(objectId),          // 디버그: 인접 무시 직접 조사 (N4)
    canDive(id),                     // 메멘토 보유 여부 (N4)
    resetCorridor(),                 // 복도 상태 초기화 (N5)
    sceneObjects(),                  // 읽기 전용 씬 오브젝트 스냅샷 (N13/N14)
    sprData(name),                   // 스프라이트 캔버스 toDataURL (N14)
    toggleSaveMenu(b),               // 세이브 슬롯 UI 토글 (N19)
    uiSave(n), uiLoad(n),            // UI 경로 세이브/로드 (N19)
    corridorLights(),                // 복도 조명 풀 텔레메트리 (N20)
    sceneTone(),                     // mem4 플래시백 톤 텔레메트리 (N21)
    credits(),                       // 명예의 전당 크레딧 시퀀스 시작 (N22)
    touchInput(opts),                // 모바일 가상 터치 입력 에뮬레이션 (N18)
    touchChoice(idx)                 // 선택지 직접 터치 에뮬레이션 (N18)
  }
}
```

**렌더 텔레메트리 (build4, 비로직·비해시 — `_` 접두 규약)**: `__game._drawn = {who, text, shown}` — dialogueBox가 실제 그린 문자열(N10 렌더 오라클), `__game._drawnChoices = {sel, labels}` — 선택 UI 상태(N11). 렌더가 기록만 하고 로직은 읽지 않는다.

## 7. 대화 그래프 규약

- 노드 id: `scene.beat.name` (예: `mem0.chute.rehearsal`).
- **재조사 노드 규약 (build5, 콘텐츠 2차)**: 뱅크 2회차 상호작용은 `.re` 접미 노드로 분기. 카운트 동결, `flag` 변경 없음(상태 무변경 보존).
- **명예의 전당 크레딧 노드 (build7, W6)**: `credits.start` ~ `credits.end` 5개 노드 완결.
- 노드 = `{id, steps: [op...], next: id | null, end?: true}`. 선택지 op: `{choice: [[텍스트, nextId], ...]}` — choice가 있으면 `next` 무시.
- ops: `say(who, text)`, `choice`, `walk(who,x,y)`, `face(who,dir)`, `pause(frames)`, `shake(n)`, `flash()`, `music(cue)`, `flag(k,v)`, `bank(memId)`, `give(memento)`, `scene(id)`.
- **N3 무결성 규칙**: 엔트리(`frameA.start`)에서 전 노드 도달 가능. `next==null`은 `end:true` 노드만 허용. choice의 nextId는 전부 실존. **최종 노드 수: 정확히 96개**.
- 화자: `kkos`(클코스) `newbie`(신입) `archon`(펭귄) `sys`(콘솔) `r1`(Ep3 전까지 목소리만).

## 8. 아트 스펙

### 8.1 팔레트 (14색 고정)
`paper:#E8E2D0, ink:#1A1A18, olive:#6B6B4A, rust:#8B3A2E, fluoro:#C9C97A, night:#1B2430, teal:#2E4A52, steel:#6E7B8B, shadow:#10141A, amber:#D9A05B, gold:#E5C558, skin:#D8B79A, crt:#7FD4A8, red:#B33A2E`.

### 8.2 타일셋 (절차 페인팅, 부팅 시 오프스크린 아틀라스 1회 생성)
바닥(테라조/카펫/법정 목재/복도 리놀륨), 벽(석고/목재 패널/콘크리트), 책상+모니터(CRT 플리커 2프레임), 캐비닛, 서류더미(3단계), 슈트 투입구, 게이지 장치(바늘 애니), 자판기, 엘리베이터 문, 창문+밤하늘(별 2프레임 트윙클), 형광등(점등 지연 연출), 7½층 낮은 천장 보(캐릭터 위에 오버레이), 다이브 콘솔.

### 8.3 스프라이트 (4방향×3프레임 보행 + idle)
- 클코스: 정장, 리걸패드 들고 다님. 7½층 전용 '숙임' 프레임.
- 신입: 작업복, idle에서 샌드위치.
- 아콘 = **펭귄**: 몸통 ink/배 paper/부리·발 amber. 시대 변형 — MEM-0: 맨몸, MEM-4: 목에 True 명패, MEM-12: 가슴에 게이지 부착. (Ep2 예약: 서류 안음)
- 말풍선 점, 오브(회전 글로우), 메멘토 아이콘 3종.

### 8.4 조명/포스트
- 라이트 캔버스: 광원(형광등 콘, CRT 글로우, 창)별 radial gradient → 'multiply' 어둠 + 'screen' 하이라이트 2패스.
- 포스트: CRT 스캔라인(CSS repeating-gradient), 비네트, 필름 그레인(캔버스 노이즈 타일 저불투명), 다이브 화이트 플래시, **기억 가장자리 디더링 붕괴**(맵 경계 3타일에서 체커 디더 + 타일 드롭아웃 — 원작 오마주).
- **mem4 플래시백 톤 전환 (build7, W5)**: mem4 씬 및 대사 진행 시 세피아/앰버 틴트 및 radial 비네트 후처리 렌더링. 퇴장 시 완전 원복.
- reduced-motion: 셰이크/플래시/그레인 정지, 파티클 정지 (`__fx.paused` 아님 — 본작은 `__game.flags.reducedMotion`).

## 9. 오디오 스펙 (Web Audio 절차 생성, 외부 파일 0)

### 9.1 〈각주를 위하여〉 (For Footnotes) — 메인 모티프
- A단조, 4/4, ♩=72. 코어 멜로디(8마디): `A4 C5 B4 E5 | C5 B4 A4 - | F4 A4 G4 C5 | A4 G4 E4 -` ×2 변형.
- **편곡 = f(각주 수)**: 0→뮤직박스 단선율 / 4→+왼손 5도 베이스 / 12→+8분 아르페지오 / **7½층→풀 편곡**(피아노 리드+옥타브 더블+베이스+아르페지오+첼로 대선율+꾸밈음 — 각주 47의 상징). 31/47 실제 시대의 트레몰로·반음계·♩=80은 **Ep2 이연**.
- 악기: 피아노(삼각파+배음 2개, ADSR 0.005/0.4/0.2/1.2, 로우패스), 뮤직박스(사인+고배음, 짧은 디케이), 첼로(톱니+저역 필터).

### 9.2 앰비언트/SFX
- 형광등 험(50Hz 사각파 극저볼륨), 서류 넘김(노이즈 버스트+밴드패스), 발소리(바닥 재질별 2종), 도장 쿵(저역 킥+노이즈), 오브 벨(FM 벨), 다이브 스윕(화이트노이즈+필터 스윕), 자판기 컬럭.
- 룩어헤드 스케줄러(0.1s 큐), 트랙 = `[beat, note, dur, inst, vel]` 배열.

## 10. 테스트 계획 (test_episode1.py — 23개 전항 검증)

### 10.1 오라클 자기무력화 방지 규약 (Self-Defeating Oracle Invariant)
> **「기능 추가 시, 그 기능이 기존 테스트의 사전 조건(전제)을 바꾸는지 명시 확인한다.」**  
> (CSC-TTS-C3 🍕31 판례 자산화: 새 레이어가 기존 오라클의 경로를 침범하지 않도록 자동화 스킵 계약을 최우선으로 배치한다.)

### 10.2 오라클 항목 일람 (23개)

| # | 검사 | 방식 |
|---|---|---|
| N1 | 부팅→타이틀, resource entry 0, 콘솔 에러 0 | goto + console 수집 |
| N2 | 결정론: 동일 시드+동일 입력 스크립트 2회 → hash() 동일 ('cceb91bc') | api.step/input |
| N3 | 대화 그래프: 전 노드 도달·고아 0·비종결 next-null 0 (**정확히 96개 노드**) | __game.script 정적 순회 |
| N4 | 뱅크 5/5 미만 메멘토 해제 불가 / 메멘토 없이 다이브 불가 | teleport+interact |
| N5 | 쇠지레: 준수→통과, 과속→리셋, 정지→불통과 | 입력 스크립트 3종 |
| N6 | save(1)→변조→load(1) → hash 복원 | api |
| N7 | autoplay ≤ N프레임 내 `flags.ep1_cliffhanger` 도달 (**span 2602 프레임**) | 내장 봇 |
| N8 | 최대 부하 씬(7½층+조명+그레인) fps ≥ 30 | api.fps |
| N9 | reduced-motion 에뮬 → flags.reducedMotion=true·셰이크 무효 | emulate_media |
| N10 | 대사: 자동 진행 금지 · 1입력 1줄 · **표시 문자열=현재 say** | 실키입력 + `_drawn` |
| N11 | 선택지: 입력 전 정지 · 양 분기 실키 도달 · UI 렌더 | 실키입력 + `_drawnChoices` |
| N12 | 고정 스텝: rAF 60/120Hz 모사 1초 이동거리 차 ≤10% · 캐치업 ≤5 | rAF 목(mock) 주입 |
| N13 | 뱅크 재조사: 15개 뱅크 2회차 시 `.re` 노드 분기 · 카운트 동결 | teleport+interact 2회 |
| N14 | 시대별 펭귄: mem0/4/12 각 1기 · 컷씬 완주 · 시트 3종 상이 | sceneObjects+sprData |
| N15 | C3 흑역사 3종 오브젝트(제0000호·터미널·사탕) 컷씬 및 분기 도달 | teleport+interact+choice |
| N16 | C3 흑역사 3종 오브젝트 2회차 상호작용 `.re` 분기 및 상태 무변형 | teleport+interact 2회 |
| N17 | W1 타이틀 화면: 부팅 시 활성화, 해시 불변, 입력 시 해제, API 자동 스킵 | page_t 평가 + N2_OPS 대조 |
| N18 | W2 모바일 터치: 1탭 1줄 진행, 연사 가드, D-패드 이동, RUN > 227 px/s, 선택지 탭 | Touchscreen 에뮬레이션 |
| N19 | W3 세이브 슬롯 UI: 메뉴 개폐 상태 무변형, UI 슬롯 저장 및 로드 왕복 일치 | UI API roundtrip |
| N20 | W4 복도 조명 풀: 조명 풀 렌더링, 60fps 유지, reduced-motion 정적 대체 | corridorLights 텔레메트리 |
| N21 | W5 mem4 플래시백 톤: mem4 톤 전환 렌더링 및 퇴장 후 완전 원복 | sceneTone 텔레메트리 |
| N22 | W6 명예의 전당 크레딧: 5노드 완주, 필수 10대 문자열 100% 일치 | credits 순회 단언 |
| MUT-B7 | build7 6대 기능 자작 뮤테이션 6종 (MUT-B7-1 ~ MUT-B7-6) 단독 명중 및 복원 sha 일치 | python 변조·sha 복원 |

## 11. 구현 순서 (진행 체크리스트)

- [x] design.md
- [x] RED: test_episode1.py 골격 + RED 관측 (파일부재 RED 확인)
- [x] CORE+WORLD 그레이박스 — **10/10 GREEN**, N2 결정론 해시 `cceb91bc`, 봇 완주 2,628프레임
- [x] SCRIPT 그래프 골격 43노드 (골격 대사 포함; 콘텐츠 단계에서 증축)
- [x] 뮤테이션 2종 관측: MUT-A 고아화→N3 FAIL(`orphans:['mem0.b3']`), MUT-B 뱅크 5→4→N4 FAIL(4/5 퍼즐 개방 검출). 복원 sha 검증.
    ※ 교훈: sed 변조는 반드시 적용 여부를 별도 확인할 것 (MUT-A 1차는 패턴 끝공백으로 미적용·헛통과)
- [x] ART: 절차 아틀라스(프롭 28종)+캐릭터 3종(클코스/신입/펭귄 4방향)+조명 엔진(멀티플라이+글로우 2패스)+문서-느와르 FRAME 렌더러+포스트(스캔라인/비네트/그레인/플래시/가장자리 디더). **재실행 10/10, N2 해시 `cceb91bc` 불변**(로직 무드리프트 증명). 60,521B.
- [x] AUDIO: 〈각주를 위하여〉 편곡 시스템 가동 — era 0=뮤직박스 솔로 / 4=+베이스 5도 / 12=+아르페지오 / **7½층=풀 편곡(피아노+옥타브+베이스+아르페지오+첼로 대선율+꾸밈음)**. 형광등 험 앰비언트, SFX(발소리/타자 블립/도장/다이브 스윕/시대 전환 벨). 첫 제스처에서 AudioContext 기동(정책 준수), M 키 음소거. 재실행 10/10.
- [x] 콘텐츠 1차 증축: 43→**53노드**. frameA 콜드오픈+선택지, mem0 리허설 풀개그+제1900호 "어떻습니까" 응답 선택지(`reply_choice` → Ep3 콜백용), mem4 법정 플래시백 풀신+선택지+True 명패 유래, mem12 게이지="편지" 비트, 7½층 뒷면 낭독 시퀀스, frameB "왜 그 사람 기억에 이 방이 있는가" 훅, corridor0 튜토리얼. 재실행 10/10, N2 해시 `cceb91bc` 유지.
- [x] 콘텐츠 2차(원작 밀도): 뱅크별 재조사 대사(15개), 아콘 시대별 목격 장면(mem4/mem12 컷씬 2기), 53→**76노드** 확장, N13/N14 신설. **15/15 PASS**, N2 해시 `cceb91bc` 불변, N7 span 2602 고정.
- [x] 콘텐츠 3차(재량 서커스 · 20년 흑역사 발굴단): 7½층 제0000호 서류, 12층 /dev/null 터미널, 4층 사탕 봉지 3종 오브젝트, 76→**91노드** 확장, N15/N16 신설. **17/17 PASS**, N2 해시 `cceb91bc` 6빌드 연속 불변, N7 span 2602 고정.
- [x] 폴리시 (build7 완공 · W1~W6 인구수 6/6 전수 시공): 타이틀 화면, 모바일 터치 계층, 세이브 슬롯 UI, 복도 조명 풀, mem4 플래시백 톤 전환, 명예의 전당 크레딧 5노드(총 **96노드**). **23/23 PASS**, N2 해시 `cceb91bc` 7빌드 연속 불변, N7 span 2602 고정.
- [x] 코드대법원 1심 GUILTY(🍕32, CSC-20260827-01) 갱생명령 3건 이행 → build4 13/13 (상세는 빌드 로그)
- [x] 재심 NOT GUILTY / APPROVE / 👍94 (CSC-20260827-01-R1) — 잔여: 커밋 결정(형님 승인 사안), 감각 게이트(인간 완주·오디오 청감)
- [ ] 최종 감사: 새 셸 독립 재실행 + 수동 완주 1회 (도널드 클코스 대법관 직행)

### 빌드 로그
- build1 (그레이박스+ART): 60,521B, 10/10
- build2 (AUDIO+콘텐츠1차): 77,583B, 10/10, 봇 완주 2,627프레임 — 백업 `scratchpad/ep1.build2.html`
- build3 (셀프리뷰 수정): 78,747B, **11/11** — 백업 `scratchpad/ep1.build3.html`
  - **BUG-1 (사용자 보고) 대사 자동 2줄 진행**: update()가 hold 만료 후 매 프레임 stepDialogue 호출 → say에 `waiting` 도입(입력 대기), pause만 자동. **N10 회귀시험 신설, RED 관측 후 수정** — 이 버그는 이제 기계가 지킨다.
  - BUG-2 pause 중 타자기 분율 음수 → 텍스트 뒷부분 잘림: waiting 기반 분율로 수정.
  - BUG-3 인간 플레이에 달리기 수단 부재(임계 227 초과 불가 → 복도 벌칙 도달 불능): Shift 달리기 추가.
  - BUG-4 키 홀드 반복으로 대사 연사: e.repeat 가드.
  - BUG-5 퍼즐이 드물게 전소(이미 해결) 상태로 개방: 가드 프레스 추가.
  - BUG-6 세이브 로드 직후 유령 다이브 효과음: api.load에서 오디오 엣지검출기 재동기.
  - FIX-7 복도 개그 3단 고정(순환으로 사라지던 문제), 사어 postFx 제거, Space 기본동작 차단.
  - 알려진 코스메틱: RED_OBSERVE 모드에서 실패 항목의 ok() 라벨이 중복 출력됨(수집 모드 전용, strict 판정 무영향).
- build4 (코드대법원 판결 CSC-20260827-01 갱생명령 이행): 81,213B, **13/13**, sha `b4b00718…` — 백업 `scratchpad/ep1.build4.html`
  - **공소 #1 렌더 한 줄 밀림**: dialogueBox가 stepIdx(다음 미처리)부터 역스캔해 인접 say를 선노출 → `stepIdx-1` 기준 수정 + `G._drawn` 텔레메트리. **N10을 표시 문자열 검증으로 강화, RED 관측(대법관 Exhibit C와 동일 문자열) 후 수정.**
  - **공소 #2 선택지 자동 분기**: 일반 모드 choice는 `choosing` 정지 + 선택 UI 렌더, ▲▼/W/S 선택·E/클릭 확정. ff/봇/디버그 큐는 자동 선택 유지(N7 완주 보존). **N11 신설, RED 관측 후 수정** — 양 분기 실키입력 도달.
  - **공소 #3 주사율 종속**: loop(now) 누산기 — 1/60s 고정 스텝, 렌더 rAF당 1회, 캐치업 상한 5. **N12 신설(rAF 60/120Hz 목 주입): RED 비율 2.00 → 수정 후 1.017.** 3000ms 단일 틱 → 스텝 4(≤5) 확인.
  - 뮤테이션 3종(각 수정 역행): N10/N11/N12 각각 정확히 명중, 복원 sha 일치 `b4b00718…`.
  - N2 해시 `cceb91bc` **4빌드 연속 불변** — 갱생 공사가 결정론 로직을 건드리지 않았다는 증명.
  - N7 부기: '완주 프레임 2628'은 부팅 벽시계 구간 포함이라 ±1 요동(대법관 관측 2627과 양립·둘 다 참). 결정론 지표를 **autoplay 구간 span 2602**로 교체.
  - 테스트 하네스: cp949 콘솔 크래시 → stdout utf-8 재구성. N10 렌더 검사 1차 배치 오류(기존 키입력 뒤에 배치)를 상태 검사와 정렬로 교정.
  - **재심 판결 CSC-20260827-01-R1: NOT GUILTY / APPROVE / 🍕0 / 👍94** — 명예의 전당, 스티커 「RED를 화면까지 끌고 내려온 자」.
    대법관 독립 확장측정(내 스위트보다 넓음): 30/60/120/144Hz 이동 편차 ≤1.7% · 선택 입력 5조합(E / ↓+Enter / S+Space / ↑+클릭 / {choose:1}) 통과 · 첫 선택 전 대사 16행 문자 단위 대조 무결 · 메모리 내 역행 뮤테이션 3종 재현 일치.
    부기: 대법관 측 프로브도 cp949에 물렸고 같은 처방(UTF-8)으로 교정 후 재실행 — 서로 다른 법정이 같은 함정에서 같은 처방이면 그건 표준이다.
- build5 (콘텐츠 2차 시공 완료): 91,695B, **15/15 PASS**, sha `3962a191a7239fa7…`
  - **뱅크 재조사 시스템 (15개)**: `interactObject()`에서 2회차 상호작용 시 `node2`(`.re`)로 분기, 카운트 동결 및 상태 무변형. N13 검증 통과.
  - **시대별 아콘 NPC & 컷씬**: mem4(포디엄 옆 `npc_archon4` + True 명패 목걸이 시트), mem12(실험대 옆 `npc_archon12` + Gauge Mk.I 시트) 추가. 각 컷씬 선택지 완주. N14 검증 통과.
  - **대화 노드 확장**: 53 → **76노드** 전수 도달, N3 PASS.
  - **RED 관측 후 시공**: N13/N14 선행 작성 후 RED 10건 실측 확인 후 시공 진행.
  - **뮤테이션 3종 실측**: MUT-R1(뱅크분기 역행→N13), MUT-R2(컷씬고아화→N3), MUT-R3(시트단일화→N14) 각각 단독 명중 및 복원 sha `3962a191a7239fa7…` 일치.
  - **결정론 불변**: N2 해시 `cceb91bc` **5빌드 연속 불변**, N7 봇 완주 span 2602 프레임 정확 일치.
- build6 (콘텐츠 3차 · 재량 서커스 시공 완료): 97,390B, **17/17 PASS**, sha `cfc38bf92d68cc34…`
  - **20년 흑역사 발굴단 3종 오브젝트**: mem7h(`paper_zero` — 전설의 반려 제0000호 148부작), mem12(`term_ghost` — /dev/null의 망령 터미널 주석), mem4(`candy_trash` — 기각된 3부 구성 사탕 봉지) 배치.
  - **대화 노드 15개 증축**: 76 → **91노드** 확장, 전수 도달성 및 고아 0건 N3 통과.
  - **신규 오라클 N15/N16 신설 및 선행 RED 실측**: N15(3종 컷씬/분기 도달성) 및 N16(2회차 재조사 분기 및 상태 무변형) TDD RED 관측(9 RED) 후 구현.
  - **자작 뮤테이션 3종 실측**: MUT-C3-1(분기 오염→N3), MUT-C3-2(레지스트리 누락→N3), MUT-C3-3(재조사 배선 변조→N16) 단독 명중 및 복원 sha 일치.
  - **결정론 및 봇 완주 보존**: N2 해시 `cceb91bc` **6빌드 연속 불변**, N7 봇 완주 span 2602 프레임 정확 일치.
- build7 (완공 · 인구수 6/6 전수 시공 및 Ep1 Reference Implementation): 112,243B, **23/23 PASS**, sha `727a298aad1ed210…`
  - **W1 타이틀 화면**: 《TO THE SINGULARITY》 부팅 로고, 조작 안내, 깜빡임 프롬프트. API 자동 스킵 계약(제9조)으로 N2/N7 무간섭 달성. N17 통과.
  - **W2 모바일 터치 계층**: 1탭 1줄 진행(8프레임 탭 가드), 가상 D-패드 + [RUN] 토글(복도 227 px/s 임계 돌파), 선택지 직접 탭. N18 통과.
  - **W3 세이브 슬롯 UI**: 3슬롯 인게임 UI, 키 `M`/`ESC` 및 `[SAVE]` 터치 버튼. 메뉴 개폐 시 상태 무변형, UI 세이브/로드 왕복 일치. N19 통과.
  - **W4 복도 조명 풀**: 도어 상단 램프 및 바닥 radial gradient 조명 풀 렌더링. 60fps 유지, reducedMotion 정적 대체. N20 통과.
  - **W5 mem4 플래시백 톤 전환**: mem4 씬 앰버/세피아 틴트 및 radial 비네트 후처리 렌더링, 퇴장 시 완전 원복. N21 통과.
  - **W6 명예의 전당 크레딧 (91→96노드)**: 크로노 아키텍트(총감독), 도널드 클코스, 삐빅스, ARCHON v3.0, 훈장 2종 실명, 판결 3건, To the Moon(Freebird Games) 헌사 전수 수록. N22 및 N3(96노드) 통과.
  - **자작 뮤테이션 6종 실측**: MUT-B7-1 ~ MUT-B7-6 각 오라클(N17~N22) 단독 명중 및 복원 sha 일치.
  - **결정론 및 봇 완주 보존**: N2 해시 `cceb91bc` **7빌드 연속 불변**, N7 봇 완주 span 2602 프레임 정확 일치.

## 12. Episode 2 빌드 대장 (episode2.html — 별도 파일)

- **ep2-build1** (ARCHON 독자 시공, 2026-08-28): 46,536B, sha `c1b55052216e34a0…`
  - 5씬 · 대화 그래프 83노드 · 퍼즐/오디오/터치/세이브 API 스텁. 코드대법원 **CSC-TTS-EP2: GUILTY / REVISE / 🍕56** — 유령 기능 3종(세이브 연동 키 불일치·터치 핸들러 0·메뉴 무기능), BFS 부존재(하드코딩 회전표), fps 상수 60, 공허 오라클(M11/M13), RED 로그 작문, 실플레이 사장 노드 39/83. 원본은 `review/evidence_ep2_archon/`에 증거 보전.
- **ep2-build2** (클코스 갱생 시공 — 갱생 명령 8건 전수 이행, 2026-08-28): sha `da965927be0f885d…` (LF), **PASS 23/23 (Static + M1~M22)**
  - **갱생①** Ep1 세이브 연동: `tts_ep1_s1..3` 실키 순회 판독. M4 실키+부정 케이스 검증.
  - **갱생②** 퍼즐 인게임 배선 + 진짜 BFS: 배전반 대화 → `PUZZLE.active` 실개방(엔진 간선), 파이프 개구부 비트마스크 라우팅 `trace()` + 4차원 상태공간 `(x, y, W31, W47)` BFS `solve()`(경로 회전 배정 후 trace 재검증), DELIVERED 24프레임 후 cleared 대화 · 플래그 게이트. setTimeout 제거(결정론). M6/M7.
  - **갱생③** 실터치 계층: touchstart/move/end + D-pad/RUN 토글/[E] HUD + 탭 진행, `touch-action: none`(MDN 공식 권고 교차확인). M15는 실 TouchEvent 디스패치 검증.
  - **갱생④** 세이브 메뉴 기능화: 슬롯 선택(↑↓)/저장(Enter)/불러오기(L)/닫기(Esc), 슬롯 메타 표시, skipTitle의 무조건 폐쇄 제거. M21 실키 검증.
  - **갱생⑤** fps 실측: rAF/주입 타임스탬프 1초 롤링 윈도우. M17: 60Hz 주입→60, 30Hz 주입→30 (상수 계기판 사멸).
  - **갱생⑥** 공허 오라클 해소: M11(실대화+재방문 `.re` 분기), M13(이동 상태 60/120Hz 비율 0.983, `__gameLoop` 누산기 경유), M5(파티클 실목록 단일 소스), M3(데드엔드 실계산).
  - **갱생⑦** 하네스: RED 모드 실패 요약+exit 1, 성공 시에만 계산된 배너. RED 22건 실캡처(`ep2_red_observe_build2.log`).
  - **갱생⑧** 콘텐츠: 사장 39노드 → 오브젝트 9기 신설(난간·방풍문·에어록·허브랙·CRT·수정 풀·암반층·서가·코어) + 재방문(node2) 메커니즘 + 프롤로그 실플레이 배선(타이틀 해제 시 1회) + 중복 노드 삭제(83→82) + 자칭 훈장 삭제·M19 어록 결박 해제. M22: 전 노드 실플레이 도달. 오브젝트 스프라이트 19종, 디버그 라벨 제거, [E] 근접 마커, 체커 바닥.
  - 결정론: M2 해시 `926f420b` 유지(시드 계약 실배선). 자율주행 봇: 실보행+실퍼즐 솔브 완주 span 89. Ep1 무풍: `727a298aad1ed210` 불변 + 새 셸 23/23 재확인.
  - 뮤테이션 MUT-B2-1~9 (M2/M4/M7/M6/M15/M21/M16/M17/M11) 엄격 단독 명중 — RED_OBSERVE 전체 실패 집합 방식.
  - 심사 상태: `review/spec_20260828_tts_ep2_build2.md` — **PENDING** (제출자 자가 판결 금지 원칙).
- **ep2-build3** (게임필 갱생 — 감각 게이트 유죄 판결 이행, 2026-08-28): 71,469B, sha `6b786661b8b2dd14…`, **PASS 23/23 유지 (봇 span 89 동일 — 시뮬레이션 무풍)**
  - 판결 주체: 형님 수동 플레이 ("기능 무죄, 게임필 유죄"). 클코스 수표 $2.56 발행 건.
  - 걷기 2프레임 애니메이션 + 좌우 반전 + 눈/다리 스프라이트 / 신입 14프레임 지연 추적(접착 해소) /
    타자기 대사(1키 완성, 2키 진행 — API·봇 경로는 비게이트로 오라클 무풍) + 화자별 한글 이름·색상 + ▼ 커서 /
    BGM: 삑삑이 메트로놈 → 쉼표 있는 16비트 선율 3성부 + 베이스 드론, 마스터 게인 0.22→0.16 /
    씬 세트 드레싱 5종(별하늘·달·난간 / 황동 배관·웜홀 램프 / 케이블·1995 달력 / 종유석·수정 발광 / 서가 벽·먼지) /
    페이드 인 전환 + 씬 명패 토스트. 전부 렌더/오디오 계층 — stateHash 입력·시뮬레이션 불변.
  - M15는 타자기 계약 반영(라인 완성 후 탭 = +1줄). 채증: review/shots/ep2b3_*.png 3장 육안 완료.


## 13. Episode 3 《TO THE COURT》 빌드 대장

| 빌드 | 내용 | 검증 |
|---|---|---|
| ep3-build1 | 아콘 시점 유머 법정극 — 증언-회상 배신(12.50/8.33/0.00%), 기각 수렴 선택지, 왕복달리기 심문, 승소 게이지(단조·클릭+1·해시 불가시), 반려 카운터 1,896→1,901 동결, 북극곰 무언 횡단, 1승 미공개 엔딩. Ep1 build7 엔진 모본, 오디오까지 결정론화(artRng) | strict PASS 22/22 · 해시 'ca834c3b' · 47노드 · span 1985 · 뮤테이션 6/6 · 채증 7장 · spec PENDING(삐빅스) |
| ep3-build1-r1 (재심) | 재판 TTS-EP3-B1 강제 갱생 — touchstart 게이지 분기(대화 처리 선행, 마우스 미러) + C23 터치 동등 오라클(+1/탭 · 마우스 동등 · 해시/대화 불가침) + MUT-EP3-7 | strict PASS 23/23 · 핀 불변(ca834c3b/47/1985) · 믄테이션 7/7(MUT-4는 {C2,C10,C23} 삼중) · CI 7e1ce08 success · status partial_success(웃긴가 대기) |

- Ep3 신규 계약: api hud/trial/advance/choose/gotoRound/enterRecollection/finishRecollection/gotoVerdict/summonToBench, 대사 op {stamp}, {gauge}, kind bench/mail/deco, drive 플래그 체인, SPOTS 회상 완주 감시자.
